{
  "name": "local-business-toolbox",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "本地商家实用工具箱 - AI团购套餐生成器（纯前端，Cloudflare Pages 就绪）",
  "engines": {
    "node": ">=18.0.0 || >=20.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "esbuild test/generator.test.mjs --bundle --format=esm --platform=node --outfile=test/.out.mjs && node test/.out.mjs"
  },
  "dependencies": {
    "vue": "^3.4.27",
    "vue-router": "^4.3.2"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.4",
    "esbuild": "^0.21.5",
    "vite": "^5.2.11"
  }
}
